<header>
  <div class="left">
    <a href="/" class="home exclude">Domů</a>
    <a href="/blog" class="blog exclude">Blog</a>
  </div>
  <div class="right">
    <button type="button" class="toggle"></button>
  </div>
</header>

<style>
  .right button {
    cursor: pointer;
  }
  header {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 32px;
  }
  .left {
    display: flex;
    flex-direction: row;
    gap: 16px;
  }
  button {
    box-shadow: 0px 1px 2px 1px rgba(0, 0, 0, 0.15);
    background-color: var(--interactive-backg);
    color: var(--main-text);
    border: none;
    border-radius: 10px;
    padding: 12px;
    width: 64px;
  }
  a {
    text-decoration: none;
    color: var(--main-text);
    padding-bottom: 4px;
    border-bottom: 3px solid var(--main-text);
    opacity: 70%;
  }
  .curr {
    opacity: 100%;
  }
</style>

<script>
  const home = document.querySelector(".home");
  const blog = document.querySelector(".blog");
  document.location.pathname == "/"
    ? home?.classList.add("curr")
    : blog?.classList.add("curr");
</script>
